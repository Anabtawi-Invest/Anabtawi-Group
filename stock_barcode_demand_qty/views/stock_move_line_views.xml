<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="stock_move_line_product_selector" model="ir.ui.view">
        <field name="name">stock.product.selector.demand.qty</field>
        <field name="model">stock.move.line</field>
        <field name="inherit_id" ref="stock_barcode.stock_move_line_product_selector"/>
        <field name="arch" type="xml">
            <field name="picking_id" position="after">
                <field name="show_barcode_demand_qty" invisible="1"/>
            </field>
            <xpath expr="//div[@name='quantity']" position="before">
                <div name="demand_quantity" class="row my-2" invisible="not show_barcode_demand_qty">
                    <div class="col">
                        <div class="text-black-50 mb-1 fs-5">
                            <label for="demand_qty" string="الكمية المطلوبة من المصنع" class="p-0"/>
                        </div>
                        <field name="demand_qty" class="text-end"
                            options="{'type': 'number'}" placeholder="الكمية المطلوبة من المصنع"/>
                    </div>
                </div>
                <div name="demand_digipad" class="row" invisible="not show_barcode_demand_qty or (tracking == 'serial' and (not hide_lot_name or not hide_lot))">
                    <widget name="digipad" field_to_edit="demand_qty"/>
                </div>
            </xpath>
            <xpath expr="//div[@name='quantity']" position="attributes">
                <attribute name="invisible">show_barcode_demand_qty</attribute>
            </xpath>
            <xpath expr="//div[@name='quantity']/div[@class='col']/field[@name='qty_done']" position="before">
                <div class="text-black-50 mb-1 fs-5">
                    <label for="qty_done" string="Quantity Done" class="p-0"/>
                </div>
            </xpath>
            <xpath expr="//widget[@name='digipad']" position="attributes">
                <attribute name="invisible">show_barcode_demand_qty or (tracking == 'serial' and (not hide_lot_name or not hide_lot))</attribute>
            </xpath>
            <xpath expr="//div[@name='quantity']/div[hasclass('col-3')]" position="attributes">
                <attribute name="invisible">1</attribute>
            </xpath>
        </field>
    </record>

</odoo>
