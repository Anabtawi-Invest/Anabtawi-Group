<odoo>

    <record id="action_bulk_reconcile_lateness_engine" model="ir.actions.server">
        <field name="name">Bulk Reconcile Lateness</field>
        <field name="model_id" ref="hr_payroll.model_hr_payslip_run"/>
        <field name="binding_model_id" ref="hr_payroll.model_hr_payslip_run"/>
        <field name="state">code</field>
        <field name="code">
records.action_bulk_reconcile_lateness_engine()
        </field>
    </record>

</odoo>
