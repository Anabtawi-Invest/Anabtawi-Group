# Fixes & Architecture Tracking Register (`factory_attendance_payroll`)

This document serves as the permanent tracking register for all resolved issues, architectural rules, and performance optimizations implemented in `factory_attendance_payroll`.

---

## 📌 Index of Solved Issues & Architectural Rules

| ID | Issue / Feature | Root Cause | Implemented Solution | File(s) |
|---|---|---|---|---|
| **FIX-01** | **Attendance Gantt View Singleton Crash** (`ValueError: Expected singleton: hr.employee(...)`) | Odoo Gantt controllers pass multi-employee recordsets (`hr.employee(id1, id2, ...)`) to `_get_versions_with_contract_overlap_with_period()`, which had `self.ensure_one()`. | Removed `self.ensure_one()`, added `super()` delegation and fallback searching `('employee_id', 'in', self.ids)`. | `models/hr_employee.py` |
| **FIX-02** | **Slow Module Activation / Upgrade Stalls** | Stored compute fields without state/date check recomputed full database history on upgrade. | Added 60-day historical cutoff in `hr_attendance.py` and filtered `_compute_attendance_reconciliation_fields` to active `draft`/`verify` slips only. | `models/hr_attendance.py`, `models/hr_payslip.py` |
| **FIX-03** | **Slow / Freezing Payslip Cancellation** | Cancelling draft slips triggered database searches across leaves and allocations, plus recursive `write({'is_reconciled': False})` loops. | Added Draft fast-path in `_revert_reconciliation_settlements` to bypass searches when never reconciled, and added `skip_reconcile_revert=True` context flag. | `models/hr_payslip.py` |
| **FIX-04** | **Slow Payslip Computation (`Compute Sheet`)** | Double deletion and recreation of `worked_days_line_ids` (once manually + once by `super().compute_sheet()`), plus unbatched work entry row-by-row writes. | Streamlined `compute_sheet()` into single-pass execution; batched rest day conversion updates into 1 bulk `to_update.write(...)`. | `models/hr_payslip.py` |
| **FIX-05** | **Work Entry Regeneration 24h Error** (`Validation Error: Duration must be positive and cannot exceed 24 hours`) | Creating/updating `hr.work.entry` records with `0.0h` duration or missing `date_start`/`date_stop` timestamps. | Enforced strict positive duration bounds ($0.01\text{h} \le \text{Duration} \le 24.0\text{h}$) and populated explicit `date_start` and `date_stop` datetimes matching the duration. | `models/hr_employee.py` |
| **FIX-06** | **Unapproved Extra Hours Appearing on Payslip** | Public holiday and daily overtime were accumulating into `attendance_gross_overtime` without verifying manager approval status. | Enforced strict check: `total_ot` only accumulates hours if `approved_ot_by_emp_date` exists OR `att.overtime_status == 'approved'` OR `att.validated_overtime_hours > 0`. | `models/hr_payslip.py` |
| **FIX-07** | **Decoupled Leave Transaction Safety** | Creating `hr.leave` / `hr.leave.allocation` during draft calculations caused database bloat and sync issues. | Calculations remain 100% in-memory during draft; database `hr.leave` and `hr.leave.allocation` creation occurs strictly inside `action_payslip_done()`. | `models/hr_payslip.py` |
| **FIX-08** | **Public Holiday Premium Rate & Zero Penalty** | Public holidays generated unworked absence deductions. | Public holidays are detected in bulk; unworked holidays have 0 penalties; worked holidays earn 150% (1.5x) rate when approved. | `models/hr_attendance.py`, `models/hr_payslip.py` |
| **FIX-09** | **4-Day Monthly Absence Grace Rule** | Unpunched days immediately generated deduction work entries. | First 4 unpunched working days per month are forgiven (grace quota); 5th+ day receives an `ABSENT` work entry. | `models/hr_employee.py` |
| **FIX-10** | **Bytecode & Pycache Management** | Compiled `.pyc` and `__pycache__` artifacts caused staging deployment conflicts. | All `__pycache__` directories and `.pyc` files are permanently excluded and cleaned. | Repository Root |
| **FIX-11** | **Out of Contract Period & Combined Undertime Calculation** | Pre-contract days were consuming grace days and artificial rest day penalties were inflating gross undertime to 30:35. | Bounded calculation to active contract dates `[eff_date_from, eff_date_to]`, updated `_get_expected_hours_on_day()` to return `0.0h` out of contract, and combined daily punch lateness with full absent work entries (`14:35 + 8:00 = 22:35`). | `models/hr_employee.py`, `models/hr_payslip.py` |
| **FIX-12** | **Fixed Shift Early Departure Undertime Capture** | Fixed shift attendance only measured check-in delay, missing early check-out departure hours. | Updated fixed schedule undertime calculation in `_compute_factory_attendance_metrics()` to capture `late_delay + early_leave` without altering flexible schedule logic. | `models/hr_attendance.py` |
| **FIX-13** | **Schedule & Break-Aware Payslip Undertime Sync** | Payslip re-calculated daily undertime with generic `8.0h - net_hrs` formula, ignoring shift schedules and employee break settings. | Updated `_compute_attendance_reconciliation_fields()` in `hr_payslip.py` to read `att.daily_undertime_hours` directly from `hr.attendance` records. | `models/hr_payslip.py` |
| **FIX-14** | **Schedule & Break-Aware Payslip Overtime Sync** | Payslip overtime re-calculated extra hours with generic math, bypassing schedule bounds for fixed shifts. | Updated `_compute_attendance_reconciliation_fields()` to read schedule-aware `daily_overtime_hours` / `validated_overtime_hours` directly from approved Attendance records. | `models/hr_payslip.py` |
| **FIX-15** | **Overtime Balance Profile Syncing** | After payslip reconciliation, employee profile overtime balance wasn't explicitly updated or restored upon cancellation. | Updated `_sync_reconciliation_settlements()` to sync `payslip.remaining_extra_hours_balance` directly to `hr.employee` profile (`total_overtime`) upon completion, and restored pre-reconciliation balance in `_revert_reconciliation_settlements()`. | `models/hr_payslip.py` |
| **FIX-16** | **Post-Reconciliation Settlement Inputs Logic** | Extra Days (`CLEAR_EXTRA`) used pre-reconciliation fallback when 0 remaining, and Annual Leave (`CLEAR_ANNUAL`) ignored Step 2 lateness deductions from leave. | Updated `CLEAR_EXTRA` to use `remaining_extra_hours_balance` directly without falsy pre-reconciliation fallback, and updated `CLEAR_ANNUAL` to subtract Step 2 lateness days (`lateness_covered_by_annual_leave / 8.0`) from pre-reconciliation leave balance. | `models/hr_payslip.py` |
| **FIX-17** | **Monday-Based Flexible Rest Day Quota** | Flexible schedule rest day quota relied on hardcoded 4-day grace in `hr_employee.py` and `worked // 6` in `hr_payslip.py`, truncating 5-Monday months to 4 rest days. | Updated `_convert_flexible_rest_days_to_ars()` in `hr_payslip.py` and `_create_absent_work_entries_for_period()` in `hr_employee.py` to count Mondays (`weekday() == 0`) in the month, granting 4 rest days in 4-Monday months and 5 rest days in 5-Monday months. | `models/hr_employee.py`, `models/hr_payslip.py` |
| **FIX-19** | **Attendance & Public Holiday Worked Days Count Alignment** | Worked days lines for `Attendance` and `Public Holiday` computed `Days` as `total_hours / 8.0` (e.g. 25.00 regular + 1.58 holiday = 26.58), while top smart button showed 26 physical attendance log days. | Updated `_get_worked_day_lines()` to set `number_of_days` for both Regular Attendance and Public Holiday lines to count distinct physical attendance log dates (`len(set(att.check_in.date() ...))`), displaying 25.00 regular days + 1.00 holiday day = 26.00 physical days total. | `models/hr_payslip.py` |
| **FIX-20** | **Flexible Retail Fixed 4-Day Rest Day Quota** | All work stations previously used the Monday-based rest day rule ($\max(4, \text{Mondays})$), giving 5 rest days to retail employees in 5-Monday months. | Differentiated `employee_work_station == 'retail'` in `_create_absent_work_entries_for_period()` (`hr_employee.py`) and `_convert_flexible_rest_days_to_ars()` (`hr_payslip.py`), enforcing a fixed 4-day monthly rest day quota for Retail while maintaining the Monday-based rule for Factory. | `models/hr_employee.py`, `models/hr_payslip.py` |
| **FIX-21** | **Combined Attendance & Paid Leaves Rest Day Calculation** | Rest day quota formula only counted physical attendance punches, excluding approved paid leave days (Annual Leave, Sick Leave) from earning rest days. | Updated `_create_absent_work_entries_for_period()` (`hr_employee.py`) and `_convert_flexible_rest_days_to_ars()` (`hr_payslip.py`) to compute `effective_attendance_days = attendance_days + paid_leave_dates`, calculating earned rest days as `effective_attendance_days // 7` (rounded down). | `models/hr_employee.py`, `models/hr_payslip.py` |
| **FIX-22** | **Pure Earned Rest Days Conversion on Payslips** | Rest day conversion on payslips used `max(num_mondays, effective_days // 7)`, which inflated rest days to 5 for employees with low attendance (e.g., 13 worked days). | Updated `_convert_flexible_rest_days_to_ars()` (`hr_payslip.py`) to calculate `allowed_rest_days = effective_attendance_days // 7`, ensuring partial-month attendance earns rest days strictly proportional to days worked ($13 // 7 = 1$, $15 // 7 = 2$). | `models/hr_payslip.py` |
| **FIX-23** | **Rest Day (ARS) Worked Day Line Display** | `_get_worked_day_lines()` previously skipped `ARS` / `REST` lines via `continue`, omitting Rest Day lines from the Worked Days table and total days count. | Updated `_get_worked_day_lines()` (`hr_payslip.py`) to populate a dedicated `Rest Day (ARS)` line displaying earned rest days (`number_of_days = ars_days`, `amount = 0.000`), bringing total payslip worked days count to 14.00 days (12 Attendance + 1 Holiday + 1 Rest Day). | `models/hr_payslip.py` |
| **FIX-24** | **6-Day Work Cycle Rest Day Formula** | Integer division by 7 (`13 // 7 = 1`) required 14 days for 2 rest days, missing the 2nd rest day earned after completing two 6-day cycles (12 worked days). | Updated formula to `effective_attendance_days // 6` in `_convert_flexible_rest_days_to_ars()` (`hr_payslip.py`) and `_create_absent_work_entries_for_period()` (`hr_employee.py`), granting 2 rest days after 12+ worked days ($13 // 6 = 2 \text{ Rest Days}$). | `models/hr_employee.py`, `models/hr_payslip.py` |
| **FIX-25** | **Rest Days Added Directly to Attendance Line** | User requested adding earned rest days directly into the Attendance line count (`number_of_days = physical_attendance + earned_rest_days`) instead of showing a separate ARS work entry row. | Updated `_get_worked_day_lines()` (`hr_payslip.py`) to add `earned_rest_days` directly to the Attendance line count, displaying 14.00 days (12 physical + 2 rest) with no extra ARS row. | `models/hr_payslip.py` |
| **FIX-26** | **Rest Day Calculation Strictly from Physical Attendance Days** | User instructed excluding public holidays and paid leaves from the rest day formula, evaluating rest days strictly from physical regular attendance check-in logs. | Updated `_get_worked_day_lines()` and `_convert_flexible_rest_days_to_ars()` (`hr_payslip.py`) and `_create_absent_work_entries_for_period()` (`hr_employee.py`) to calculate `earned_rest_days = physical_attendance_days // 6` strictly from physical attendance check-ins. | `models/hr_employee.py`, `models/hr_payslip.py` |
| **FIX-27** | **Unified Physical Attendance Rest Day Formula for Retail & Factory** | Retail work station previously used a fixed 4-day rest day rule, overriding `physical_attendance_days // 6` and inflating Attendance days to 27.00. | Unified the Attendance line formula across both Retail and Factory to `physical_attendance_days // 6`, producing 25.00 Attendance days (22 physical + 3 rest) for 22 physical attendance check-ins regardless of work station. | `models/hr_employee.py`, `models/hr_payslip.py` |
| **FIX-28** | **Total Physical Attendance Check-ins Count Alignment** | Physical attendance count deducted public holiday check-in dates ($8 - 1 = 7$), reducing earned rest days to 8.00 days instead of counting all 8 physical check-ins. | Updated `physical_attendance_days` in `_get_worked_day_lines()` (`hr_payslip.py`) to use total physical attendance check-ins (`len(set(att.check_in.date() for att in attendances))`), producing 9.00 Attendance days ($8 + 1 = 9$). | `models/hr_payslip.py` |

---

## 🔒 Architectural Rules Never to Break

1. **No Database Writes Inside Compute Methods:**
   Never call `write()`, `create()`, or `unlink()` inside `@api.depends` compute methods. Compute methods must only assign values to fields in memory.
2. **No Singleton Assumptions on Gantt/Planning Hooks:**
   Methods invoked by Gantt views or calendar controllers must always handle multi-record recordsets (`self` containing multiple employees/records).
3. **Draft Payslip Fast-Paths:**
   Cancelling or recalculating draft payslips must never perform expensive historical table scans across `hr.leave` or `hr.attendance.overtime.line`.
4. **Work Entry Bounds Protection:**
   Always ensure `0.01 <= duration <= 24.0` and populate valid `date_start` and `date_stop` whenever creating `hr.work.entry` records.
5. **Approval Enforcement on Payslips:**
   Never show or pay overtime/extra hours unless `overtime_status == 'approved'` or `validated_overtime_hours > 0`.
