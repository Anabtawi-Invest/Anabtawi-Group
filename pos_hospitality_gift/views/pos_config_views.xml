<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="pos_config_view_form_inherit_pos_hospitality_gift" model="ir.ui.view">
        <field name="name">pos.config.form.inherit.pos.hospitality.gift</field>
        <field name="model">pos.config</field>
        <field name="inherit_id" ref="point_of_sale.pos_config_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//setting[field[@name='module_pos_hr']]" position="after">
                <setting
                    id="hospitality_accounting_setting"
                    string="Hospitality Gift Expense"
                    help="Expense account used for stock cost of gifted POS lines."
                >
                    <div class="content-group mt16">
                        <label for="gift_expense_account_id" string="Hospitality Expense" class="fw-normal me-1"/>
                        <field name="gift_expense_account_id"/>
                    </div>
                </setting>
            </xpath>
        </field>
    </record>
</odoo>
