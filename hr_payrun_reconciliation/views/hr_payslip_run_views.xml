<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_hr_payslip_run_form_inherit_reconciliation" model="ir.ui.view">
        <field name="name">hr.payslip.run.form.reconciliation</field>
        <field name="model">hr.payslip.run</field>
        <field name="inherit_id" ref="hr_payroll.hr_payslip_run_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header" position="inside">
                <button
                    name="action_reconciliation"
                    type="object"
                    string="Reconciliation"
                    class="btn-primary"
                    attrs="{'invisible': [('state', '!=', 'draft')], 'readonly': [('reconciliation_done','=',True)]}"
                />
                <button
                    name="action_reset_reconciliation"
                    type="object"
                    string="Reset Reconciliation"
                    class="btn-secondary"
                    attrs="{'invisible': ['|', ('state', '!=', 'draft'), ('reconciliation_done','=',False)]}"
                />
            </xpath>

            <xpath expr="//sheet" position="inside">
                <group>
                    <field name="reconciliation_done" invisible="1"/>
                </group>

                <group string="Reconciliation" col="4" attrs="{'invisible': [('reconciliation_count','=',0)]}">
                    <button name="action_view_reconciliation_lines" type="object" class="oe_stat_button" icon="fa-check-square-o">
                        <field name="reconciliation_count" widget="statinfo" string="Lines"/>
                    </button>
                </group>

                <notebook position="inside">
                    <page string="Reconciliation Lines" attrs="{'invisible': [('reconciliation_count','=',0)]}">
                        <field name="reconciliation_ids" readonly="1" context="{'default_pay_run_id': active_id}">
                            <tree>
                                <field name="employee_id"/>
                                <field name="lateness_hours"/>
                                <field name="ot_used_hours"/>
                                <field name="annual_used_hours"/>
                                <field name="late_unpaid_hours"/>
                                <field name="execution_datetime"/>
                                <field name="executed_by"/>
                                <field name="state"/>
                            </tree>
                        </field>
                    </page>
                </notebook>
            </xpath>
        </field>
    </record>
</odoo>
